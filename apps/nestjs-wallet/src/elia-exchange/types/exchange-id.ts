/**
 * Enumerates the types of workflows provided
 * Note that this is isn't necessarily a standard enumeration
 * They could, for instance, be workflows defined in the vc-api-use-cases
 * {@link https://w3c-ccg.github.io/vc-api-use-cases}
 * But they could very well not be standard as well.
 */
export enum ExchangeId {
  permanent_resident_card_issuance = 'permanent-resident-card-issuance'
}
