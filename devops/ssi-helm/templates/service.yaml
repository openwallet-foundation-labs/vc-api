apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssi-helm.fullname" . }}
  labels:
    {{- include "ssi-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssi-helm.selectorLabels" . | nindent 4 }}
